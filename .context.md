# 🧠 AI SYSTEM INSTRUCTIONS FOR STREAM-GATEWAY
1. **Protobuf Otoritesi:** Gelen veriyi çözerken daima `snake_case` field isimlerini koru (Rust/JS uyuşmazlığına dikkat).
2. **Session Authority:** `trace_id` ve `session_id` her zaman istemciden (SDK) beklenir. Yoksa kendisi üretir.
3. **Ghost Publisher:** RabbitMQ kopsa bile WebSocket oturumu düşmemelidir (In-memory buffering).
